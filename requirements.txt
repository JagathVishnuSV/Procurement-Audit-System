# ─────────────────────────────────────────────────────────────────────────────
# Procurement Audit System – Python Dependencies
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Sprint 1: Data Foundation & Infra ─────────────────────────────────────────

# ORM & Database
SQLAlchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9       # Sync driver (used by Alembic + sync queries)
asyncpg==0.29.0              # Async driver (used by async SQLAlchemy sessions)

# Configuration
pydantic==2.7.0
pydantic-settings==2.2.1

# HTTP Client (async, used for USAspending API + future LLM calls)
httpx==0.27.0
tenacity==8.3.0              # Retry / circuit breaker logic

# Event Streaming (Redpanda / Kafka)
aiokafka==0.10.0

# Feature Store / Cache
redis[asyncio,hiredis]==5.0.4

# Utilities
python-dotenv==1.0.1
loguru==0.7.2                # Structured, leveled logging
python-dateutil==2.9.0

# ── Sprint 2: ML Layer ─────────────────────────────────────────────────────────
scikit-learn==1.5.0
shap==0.45.0
xgboost>=2.0.0
numpy==1.26.4
pandas==2.2.2
joblib==1.4.2                # Model serialization
networkx==3.3                # Graph-based fraud detection

# ── Sprint 3: RAG / CLM Pipeline ──────────────────────────────────────────────
langchain==0.2.1
langchain-community==0.2.1
faiss-cpu==1.8.0
sentence-transformers==3.0.0  # all-MiniLM-L6-v2 (local embeddings)
pypdf==4.2.0                  # PDF loading/chunking

# ── Sprint 4: LLM Orchestration ────────────────────────────────────────────────
langchain-groq==0.1.5
langchain-google-genai==1.0.5
google-generativeai==0.5.4
instructor==1.2.3             # Enforce strict JSON outputs from LLMs
groq==0.9.0

# ── Sprint 5: FastAPI Backend ──────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9       # File uploads (PDF contract ingestion)
websockets==12.0              # WebSocket support for live UI updates

# ── Testing ────────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
factory-boy==3.3.0            # Test fixtures / data factories
Faker==25.2.0                 # Realistic fake data generation
